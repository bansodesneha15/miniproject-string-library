#include<stdio.h>
#include<stdlib.h>
#include "string_lib.h"

char* string_dup(char *);

char* string_dup(char *s1){

int l = string_length(s1);
char s2[l];

string_copy(s2, s1);

return ;
}









































